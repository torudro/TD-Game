import pygame
import pytmx
import tower_info

pygame.init()

class Tower_Type:
    def __init__(self, price, resell,  speed, radius,  image):
         self.price = price
         self.resell = resell
         self.speed = speed
         self.radius = radius
         self.image = image

class Enemy:

    def __init__(self, tower_type):
        self.destroy = False
        self.price = tower_type.price
        self.resell = tower_type.resell
        self.speed = tower_type.speed
        self.radius = tower_type.radius
        self.image = tower_type.image

    def draw(self):

        